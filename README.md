# jev-profanity

Lab benchmark: detect + asterisk-censor unsafe text for a company external-comms filter, comparing TypeSafe Jev vs lexicon vs obfuscation-aware hybrid.

See full README after initial file sync. Requires `TYPESAFE_API_KEY` in env for Jev runs — never commit secrets.
